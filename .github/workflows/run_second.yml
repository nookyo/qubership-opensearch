name: Second Workflow

on:
  pull_request:

permissions:
  actions: read
  contents: read

jobs:
  wait-for-first:
    runs-on: ubuntu-latest
    steps:
      - name: Wait for First Workflow
        uses: netcracker/qubership-workflow-hub/actions/wait-for-workflow@399-feat-add-composite-action-for-workflow-dispatch-in-pr-builds
        with:
          workflow: "First Workflow"
          ref: ${{ github.head_ref }}

  second:
    needs: wait-for-first
    runs-on: ubuntu-latest
    steps:
      - run: echo "🚀 Running Second Workflow after First"



