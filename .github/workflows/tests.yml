name: Second Workflow

on:
  pull_request:

permissions:
  actions: read
  contents: read

jobs:
  wait-for-first:
    runs-on: ubuntu-latest
    steps:
      - name: Wait for First Workflow
        uses: lucasssvaz/wait-on-workflow@v1.0.0
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          workflow: "First Workflow"
          max-wait: 1
          repository: ${{ github.repository }}
          interval: 10
          timeout: 300

  second:
    needs: wait-for-first
    runs-on: ubuntu-latest
    steps:
      - run: echo "🚀 Running Second Workflow after First"
